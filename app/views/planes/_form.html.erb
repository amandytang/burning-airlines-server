<%= form_with(model: plane, local: true) do |form| %>
  <% if plane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plane.errors.count, "error") %> prohibited this plane from being saved:</h2>

      <ul>
      <% plane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :column %>
    <%= form.number_field :column, id: :plane_column %>
  </div>

  <div class="field">
    <%= form.label :row %>
    <%= form.number_field :row, id: :plane_row %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
